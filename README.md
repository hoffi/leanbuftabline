# Lean Buffer Tabline (leanbuftabline)

A Simple Tabline-Plugin that only shows your open buffers in the tabline.

Shows the number, name and modified indicator. It also supports mouse clicks in
neovim.
